# src/render

D3D11 renderer, SceneBuilder, GlyphAtlas, DirectWrite FontCache, IconMap (sf: names to Segoe Fluent Icons), GPU instance ABI (must match shaders/ybar.hlsl byte-for-byte). Spec section 7.
