# src/app

Daemon lifecycle: role dispatch, message loop, config discovery/exec, hotload (ReadDirectoryChangesW), plus the in-daemon power/battery, system_stats and front-app publishers. Also the CLI client role, the local `autostart`/`theme` verbs, ScriptRunner (shell resolution, job-object watchdog), and the JSONC config tier. Spec sections 5, 10.1, 12.
