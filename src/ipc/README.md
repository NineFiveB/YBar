# src/ipc

AF_UNIX-over-Winsock socket (single-instance lock), byte-identical wire format, domain-batch CommandParser, CommandHandler (every verb with reference usage/error strings, platform effects injected). Spec sections 3.1, 3.2, 5.1, 9.
