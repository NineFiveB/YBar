# src/providers

Audio (WASAPI endpoint volume + per-app sessions), network, media (GSMTC), app display names (app_info), the app_launched/app_terminated process-diff fallback, tray icons, the window list, and the komorebi and YTile WM subscription providers. Power, system stats and the foreground hook live in app/daemon.cpp. Spec sections 10-11.
