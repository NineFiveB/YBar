# komorebi + whkd pairing

Replaces the macOS yabai-skhd example: komorebi.json fragment (do NOT set
global_work_area_offset when ybar reserve=komorebi is active - ybar sends
MonitorWorkAreaOffset itself), whkd bindings firing "ybar --trigger ...", and
the pairing walkthrough. Spec section 11.6.
